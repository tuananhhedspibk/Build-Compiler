Program Example1; (* Factorial *)
   
Var a : INTEGER;
    b : INTEGER;
    c : CHAR;
    x : INTEGER;
BEGIN
	a,b,c := 1,2,'a';
	x := -3;	
	(*x := if x = 0 then 0 else if x > 0 then x else -x;*)
	(*x := IF x > 0 THEN x ELSE -x * IF x > 0 THEN x ELSE -x;*)
	CALL WRITEI(b);
END. (* Factorial *)
